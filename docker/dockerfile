FROM ubuntu:latest

RUN apt-get update && apt-get install -y wget \
    && wget -O /usr/local/bin/tianlicdn https://github.com/Tianli-CDN/cdn-server/releases/download/1.7/tianlicdn-linux-amd64 \
    && chmod +x /usr/local/bin/tianlicdn

WORKDIR /app

CMD ["tianlicdn"]

EXPOSE 5012